// argument[0], argument[1] = start point (x1, y1)
// argument[2], argument[3] = end point (x2, y2)
// argument[4] = x,
// return y;

var m = (argument[3] - argument[1]) / (argument[2] - argument[0]);

return ( m*(argument[4] - argument[0])); 
